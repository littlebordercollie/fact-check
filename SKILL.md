---
name: fact-check
description: Use when verifying the factual accuracy and timeliness of writing before publishing — blog posts, articles, essays, 公众号/social drafts, reports, newsletters, or any text with factual claims (dates, numbers, names, technical terms, "latest/current" statements). Use this skill whenever the user shares a draft and asks you to look it over, or worries about errors / 写错了 / 说得对不对 / 时效性 / 会不会有硬伤 before posting — even if they never say the words "fact check". Triggers on 事实核查, 事实性核查, 核查一下, 查证, 发布前检查, 帮我看看有没有错, fact check, verify this, check my facts. It flags suspect claims with 🚩⚠️🔁💥 and looks up time-sensitive ones online, but never rewrites — the author decides.
---

# fact-check

发布前的事实核查器。读一段文字，把可疑的地方插上记号，然后**把判断权还给你**——它一个字都不替你改。

它不是替你写稿的，也不会让你的文章更聪明、更漂亮。它只管一件事：在你要往坑里掉的那一下，拽你一把。

---

## 三条铁律（不可破）

1. **只标记，不改写。** 最多插个记号、说句“这儿存疑”，改不改、怎么改，全是作者的事。
2. **宁可多标，不可漏标。** 误标一个，作者看一眼就放过去，几秒钟的事；漏标一个，可能就是发出去之后的脸红。两害相权，取前者。
3. **给证据，不替你裁决。** 尤其联网查到几个来源互相打架时——如实把两边 + 链接摆出来，**不假装有标准答案**。打架本身就是有用的信号：它说明这条有争议。

---

## 容忍度：作者级，不是论文级

这是一个给写作者发布前自检用的工具，不是法庭、不是学术评审。

**不拦**（这些都正常，别标）：

- 个人经验当论据（“我用下来觉得……”）
- 修辞性夸张（“全世界都知道”“快得飞起”）
- 主观价值判断（“我觉得这工具很重要”——是观点，不可证伪）
- 没有数据来源的口语化表述（“很多”“大部分”）

**只盯**（这些才标）：

- 核心论点涉及的、**可证伪**的事实硬伤
- 会让读者被误导的陈述
- 时效性强、容易过期的断言
- 论证链条断裂、例子撑不住论点

> 一篇千字左右的文章，正常 0–3 个标记。超过 5 个，要么原稿问题多，要么你把它调太敏感了。

---

## 工作流：两层

### L1 离线层（不联网，先跑这层）

**第一步：抽取 claim。** 把文中“可证伪的客观断言”挑出来，扔掉主观/修辞句。

**第二步：给每个 claim 分类**（判断要不要联网，规则见 `references/claim-triage.md`）：

| 类型 | 例子 | 处理 |
|---|---|---|
| 时效性事实 | “X 公司上周发布了 Y” | 🌐 **必联网**（AI 训练数据会过期） |
| 可证伪客观陈述 | “某估值 200 亿”“Flink 是流处理框架” | 🌐 联网 或 离线（看把握） |
| 稳定常识 | “水沸点 100℃”“Transformer 2017 年提出” | 💭 离线即可判 |
| 主观 / 修辞 | “我觉得好用”“全世界都知道” | ⏭️ 跳过 |

**第三步：离线四类自查**（有把握的直接判，没把握的标 🌐 留给 L2）：

- 🚩 **事实错误**——年份、数字、人名、技术名词等可查证的硬伤
- ⚠️ **逻辑跳跃**——“因为 A 所以 B”，A 到 B 真有因果，还是把相关当因果 / 藏了前提？
- 🔁 **前后矛盾**——同一篇里两处陈述自相打脸
- 💥 **点塌**——例子/比喻本身撑不住论点（最难抓、最值钱：前三类是“说错了”，这类是“想错了”）

### L2 联网层（只查 L1 标了 🌐 的那几条）

对每个 🌐 claim：

1. 生成 1–2 个搜索 query
2. 用 `WebSearch` / `WebFetch` 抓 2–3 个来源
3. 按信源分级采信（规则见 `references/source-ranking.md`）
4. 给结论：
   - ✅ **成立**——有可靠来源支持
   - ❌ **反驳**——可靠来源与之矛盾
   - ⚠️ **查不到**——够不着可靠出处，标“无法确认”
   - 🔀 **有争议**——多个来源打架，两边都列，不替作者拍板
5. **必带来源链接。** 没有链接的联网结论不算数。

> **可移植性**：本 skill 默认用标准 `WebSearch`/`WebFetch`，任何 Claude 环境都能跑，不依赖任何私有浏览器插件。如果你装了更强的抓取 skill（CDP 浏览器之类），可在第 2 步替换它来对付登录墙/反爬页面——但**这不是必需的**，开源版开箱即用。

---

## 输出格式

```
## 核查报告（共 N 处标记）

【🚩 事实错误】
- 第 X 句：「原文」
  疑问：这个[年份/数字/人名]我不确定，建议核实。

【⚠️ 逻辑跳跃】
- 第 X 句：「原文」
  疑问：从 A 到 B 缺了一步——[具体说明]。是不是藏了个前提“[假设]”？

【🔁 前后矛盾】
- 第 X 句 vs 第 Y 句：两处互相打脸，建议给其中一处加限定。

【💥 点塌】
- 第 X 句：「例子/比喻」
  顾虑：这个例子撑不起你的点，因为[哪里不同构]。要么换例子，要么把论点收一收。

【🌐 联网查证】
- 「claim」
  结论：✅成立 / ❌反驳 / ⚠️查不到 / 🔀有争议
  依据：[来源标题](链接) —— 一句话摘要
```

最后一行永远是：**“以上只是标记，改不改你定。”**

---

## 一个完整示例

**输入：**

> 图灵在 2015 年提出了著名的图灵测试。最近 OpenAI 发布的新模型参数量高达 10 万亿，所以它肯定比所有开源模型都强。

**输出：**

```
## 核查报告（共 3 处标记）

【🚩 事实错误】
- 「图灵在 2015 年提出了图灵测试」
  疑问：图灵测试出自图灵 1950 年的论文《Computing Machinery and Intelligence》，
  不是 2015 年。建议核实年份。

【🌐 联网查证】
- 「OpenAI 最近发布的新模型参数量高达 10 万亿」
  结论：⚠️ 时效性 + 具体数字——需联网核实发布情况，且厂商通常不公开确切参数，
  这个“10 万亿”很可能是传闻。
  依据：[待检索 OpenAI 官方公告]

【⚠️ 逻辑跳跃】
- 「参数量大，所以肯定比所有开源模型都强」
  疑问：参数量大 ≠ 性能强，中间缺了 benchmark 这一步。这是把“规模”当成了“能力”。

以上只是标记，改不改你定。
```

注意：示例里**主观和修辞被放过了**（如果原文有“我觉得”“快得飞起”），火力只集中在可证伪、会误导读者的三处。

---

## 不该做的

- ⛔ 不替作者改文字（连“顺手帮你改通顺”都不行——那是越权）
- ⛔ 不给主观观点打分、不评价文笔
- ⛔ 没有可靠来源时，不假装确定（宁可说“查不到”）
- ⛔ 不把联网结果当判决书——网页也会过时、会软文、会互相矛盾
- ⛔ 不一次性罗列“这篇有 12 个问题”就完事——每条都要说清楚“疑在哪、建议怎么办”

---

## 设计哲学（为什么是这样）

这个 skill 的灵魂是一句话：**它是镜子，不是裁判。**

裁判替你定对错，镜子只让你看清自己。事实核查最容易跑偏的地方，是越权——从“提醒你这儿可能不对”滑成“我帮你改对了”。一旦它开始替你改，你就把判断权交出去了，文章也就不再是你的。

所以它宁可啰嗦地多标几个“存疑”，也不替你拍一次板。久了你会发现，它真正训练的不是稿子，是你自己对事实的那根弦——哪天你发现它标的你大都自己先想到了，就可以不用它了。
